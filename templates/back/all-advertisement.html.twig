{% extends 'back/base.html.twig' %}

{% block title %}Listes de tout les annonces publier sur le bon point{% endblock %}

{% block body %}
<div class="espace40"></div>
    <div class="container">
    <h1 class="h1 blue-text lighten-1 text">Listes de tous les annonces publier récemment sur le bon point.</h1>
        <div>Nombres total d'annonces : 5 </div>
        <div>Nombres d'annonces validé : 15 </div>
        <div>Nombres d'annonces en attente de validation validé : 15</div>
    </div>
    <div class="container">
        {% for row in advertisements|batch(3) %}
        <div class="row">
            {% for ad in row %}
            <div class="col s12 m12 l4 xl4">
                <div class="card">
                    <div class="card-image">
                        <img class="responsive-img" src="https://picsum.photos/1000/700">
                        <span class="card-title">{{ ad.title }}</span>
                    </div>
                    <div class="card-content">
                        <p>I am a very simple card. I am good at containing small bits of information. I am convenient because I require little markup to use effectively.</p>
                    </div>
                    <div class="card-action">
                        <a href="#">This is a link</a>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
        {% endfor %}
    </div>
{% endblock %}